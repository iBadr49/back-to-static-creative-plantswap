// place files you want to import through the `$lib` alias in this folder.
export { default as Button } from '$lib/componenten/Button.svelte';
export { default as Title } from '$lib/componenten/Title.svelte';
